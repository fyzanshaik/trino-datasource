connector.name=postgresql
connection-url=jdbc:postgresql://postgres:5432/trino
connection-user=trino
connection-password=trino
# Required so mixed-case / quoted PostgreSQL identifiers (e.g. "qa-with-dash"."Table With Spaces")
# are queryable through Trino, not only listed in jdbc.tables.
case-insensitive-name-matching=true
