.env
*.pyc
__pycache__/
.venv/
.idea/
.vscode/

# The mapping is the only artifact that links anonymized names back to the
# original source — must never be committed. Anonymized DDL is fine.
replay/identifier-mapping.json
